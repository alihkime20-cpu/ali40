# التقرير الشامل لموقع SABACUN

**تاريخ التقرير:** 23 أغسطس 2026  
**اسم المشروع:** SABACUN  
**الإصدار المنشور في بيئة WebDev:** `3f12ee122c5285b173dccafb8c2e14797bf7361d`  
**رابط المعاينة:** https://3000-i3skpsit18wq8ttbqu84r-6a5d7a52.us5.manus.computer  
**النطاق المنشور:** https://globalnews-zkgmaf9k.manus.space

## 1. الملخص التنفيذي

تم تحويل SABACUN من مشروع سابق متعدد الوظائف إلى منصة مجتمعية عربية بواجهة RTL، تركز على **حملات التبرع بالدم والتطوع**. صُممت الواجهة بهوية مستقلة تعتمد اللون الأخضر المزرق مع الذهبي والأحمر لحالات التبرع بالدم، وتضم تغذية حملات، بحثاً، تصفية حسب النوع، صفحات تفاصيل، التسجيل في الحملات، نشر الحملات، وصفحة حساب شخصية.

النسخة الحالية تعمل على مستوى الواجهة وتستخدم بيانات داخلية وتخزيناً محلياً في المتصفح لبعض التفاعلات. لذلك فهي مناسبة حالياً كواجهة وتجربة استخدام قابلة للمراجعة، لكنها ليست بعدُ منصة إنتاجية متعددة المستخدمين ما لم تُربط بقاعدة بيانات ومصادقة ومراجعة حقيقية.

## 2. الهوية والتصميم المرئي

تم إنشاء هوية SABACUN على أساس منصة مجتمعية عربية مستقلة، مع استخدام شعار نصي باسم **SABACUN** وعلامة حرف `S`. التصميم متجاوب ويستخدم اتجاه RTL، ومساحات واسعة، وبطاقات ذات حواف دائرية، وخلفية شبكية خفيفة في الواجهة الرئيسية، وألواناً ذات دلالات واضحة.

| العنصر | التنفيذ الحالي |
|---|---|
| الاتجاه واللغة | العربية RTL في صفحات المنصة |
| اللون الأساسي | أخضر مزرق داكن للهوية والتنقل والأزرار الرئيسية |
| اللون المساعد | ذهبي للعناوين واللمسات البصرية |
| التبرع بالدم | لون أحمر هادئ وبطاقات ذات خلفية وردية فاتحة |
| التطوع | أخضر فاتح مع أيقونة الأشجار |
| سطح المكتب | شريط علوي، تنقل مركزي، بطاقات شبكية وتذييل |
| الهاتف | قائمة هاتفية، زر قائمة، وشريط تنقل سفلي ثابت |
| الوصول الداخلي | روابط عودة في الصفحات الداخلية وصفحات المعلومات |

## 3. الصفحات والمسارات الحالية

| المسار | الوظيفة |
|---|---|
| `/` | الصفحة الرئيسية وتغذية الحملات والبحث والتصفية |
| `/blood` | صفحة طلبات التبرع بالدم وبطاقات الاستجابة |
| `/volunteer` | صفحة فرص التطوع وبطاقات الحملات |
| `/publish/blood` | صفحة مستقلة لنشر طلب تبرع بالدم |
| `/publish/volunteer` | صفحة مستقلة لنشر فرصة تطوع |
| `/create` | مسار عام متوافق لصفحة نشر الحملة |
| `/campaign/:id` | تفاصيل الحملة والتسجيل أو إلغاء التسجيل |
| `/profile` | الحساب الشخصي وملخص المشاركات والحملات المنشأة |
| `/about` | صفحة من نحن |
| `/privacy` | سياسة الخصوصية |
| `/terms` | شروط الاستخدام |
| `/cookies` | سياسة ملفات الارتباط |
| `/content-policy` | سياسة المحتوى |
| `/contact` | التواصل بالبريد وواتساب الأعمال |
| المسارات غير المعروفة | صفحة 404 عربية داخل التخطيط العام |

تمت إزالة مسارات الأدوات القديمة من التوجيه الحالي، ومنها مسارات ضغط الصور وتحويل الصيغ ودمج PDF وتحويل الصور إلى PDF وإزالة الخلفية. المسار `/tools/image-compressor` يعرض صفحة غير موجودة وفق اختبار المتصفح الحالي.

## 4. الصفحة الرئيسية والتغذية

تبدأ الصفحة الرئيسية برسالة واضحة: **«شارك في حملة، واترك أثراً.»** وتعرض دعوات مباشرة إلى نشر حملة أو التبرع بالدم. أسفل المقدمة توجد تغذية الحملات مع حقل بحث عربي وتصنيفان فعليان هما **التبرع بالدم** و**التطوع**، بالإضافة إلى خيار **الكل**.

البحث يعمل على العنوان والوصف والموقع، ويدعم النص العربي والمسافات، ويعرض رسالة عند عدم العثور على نتائج. كل حملة تظهر في بطاقة تحتوي على نوع الحملة، العنوان، الوصف، الموقع العام، التاريخ، الوقت، عدد المشاركين، والناشر، مع زر للتسجيل.

## 5. التبرع بالدم

صفحة `/blood` تعرض طلبات مرتبطة بفصائل الدم مع حالة كل طلب، والجهة الصحية، وموقع عام يحدده مقدم الطلب، وتاريخ الحاجة، وعدد المستجيبين. الحالات الحالية هي **مطلوب** و**تم توفير متبرعين** و**مكتمل**.

يوجد تحذير توعوي يطلب من المستخدم التأكد من ملاءمة حالته الصحية والتواصل مع الجهة المعنية قبل التوجه. زر **«أريد التبرع»** يغيّر حالته إلى **«شكراً لاستجابتك»** في الواجهة الحالية.

## 6. التطوع

صفحة `/volunteer` تعرض فرص التطوع المتاحة، مثل حملة تشجير وتنظيف مساحة عامة. تم إزالة أسماء المحافظات من المواقع، وأصبحت المواقع العامة بصياغة مثل **«موقع يحدده الناشر»** أو **«الموقع العام · نقطة التجمع»**.

تحتوي الصفحة على زر مستقل **«انشر فرصة تطوع»** يفتح `/publish/volunteer`، وتعرض بطاقات الحملات مع تفاصيلها وإمكانية التسجيل.

## 7. نشر الحملات

تم إنشاء صفحتين منفصلتين للنشر حسب القسم:

- `/publish/blood` لنشر طلب تبرع بالدم.
- `/publish/volunteer` لنشر فرصة تطوع.

النموذج يتضمن نوع الحملة، العنوان، الوصف، الموقع، التاريخ، الوقت، وعدد المشاركين المطلوب عند الحاجة. توجد ضوابط للحقول الأساسية ورسالة عربية عند الإرسال الناقص. بعد الإرسال تُحفظ الحملة محلياً في المتصفح بحالة **مراجعة**، وتظهر رسالة تفيد بإرسالها للمراجعة.

هذه العملية ليست بعدُ مراجعة إدارية حقيقية ولا تُرسل البيانات إلى خادم أو قاعدة بيانات. لذلك يجب عدم اعتبار الإرسال الحالي طلباً منشوراً على مستوى جميع المستخدمين.

## 8. تفاصيل الحملة والتسجيل

مسار `/campaign/:id` يعرض صفحة تفصيلية للحملة تشمل الصورة التوضيحية، النوع، العنوان، الوصف، الموقع، التاريخ، الوقت، الناشر، تفاصيل إضافية، وعدد المشاركين.

التسجيل يعمل محلياً عبر `localStorage`. يستطيع المستخدم الضغط على **«سجل الآن»**، ثم تظهر حالة **«مسجل»** ورسالة نجاح. الضغط مرة ثانية يلغي التسجيل ويعيد الزر إلى حالته السابقة. هذه الحالة محفوظة على جهاز المتصفح نفسه وليست حساباً مركزياً.

## 9. الحساب الشخصي

صفحة `/profile` تعرض هوية زائر SABACUN وملخصاً للحملات المنشأة والمشاركات المسجلة، مع روابط نشر وتنقل مناسبة للهاتف. الحساب الحالي ليس نظام تسجيل دخول؛ الاسم المعروض ثابت على مستوى الواجهة، والبيانات مستمدة من التخزين المحلي.

## 10. البيانات الحالية ونطاقها

يستخدم المشروع ملف `client/src/lib/community.ts` لتعريف الأنواع والبيانات الأساسية. الأنواع الحالية هي:

| النموذج | الحقول الأساسية |
|---|---|
| `Campaign` | النوع، العنوان، الوصف، الموقع، التاريخ، الوقت، المشاركون، الناشر، الحالة |
| `BloodRequest` | فصيلة الدم، الجهة، الموقع، تاريخ الحاجة، التفاصيل، الحالة، المستجيبون |
| المستخدم الحالي | اسم زائر ثابت، نبذة، أحرف أولى |

البيانات المنشورة الحالية داخل الواجهة ليست بيانات مستخدمين حقيقيين متزامنة بين الأجهزة. الحملات التي ينشئها المستخدم والتسجيلات تحفظ محلياً في مفاتيح `localStorage` الخاصة بالموقع.

## 11. ما تم حذفه أو تغييره

تم حذف قسم **«وجبات»** وقسم **«مبادرة مجتمعية»** من التصنيفات والواجهات والمسارات. كما أزيلت أسماء المحافظات من بيانات الحملات والطلبات، واستُبدلت بمواقع عامة يمكن للناشر تحديدها.

تم أيضاً تنظيف العبارات التي تصف المنصة أو الحملات بأنها تجريبية، وإزالة مصطلحات `Mock` من أسماء البيانات والاختبارات. لا تظهر حالياً عبارات مثل **«نسخة تجريبية»** أو **«حملة تجريبية»** أو `Mock` في ملفات الواجهة والاختبارات التي تمت مراجعتها.

## 12. الخصوصية والمعلومات العامة

توجد صفحات سياسة الخصوصية وشروط الاستخدام وسياسة ملفات الارتباط وسياسة المحتوى والتواصل. ويظهر في صفحة التواصل:

- **المسؤول التحريري:** مهندس علي.
- **البريد الرسمي:** `alihkime20@gmail.com`.
- **واتساب الأعمال:** `9647740669189`.

يوجد إشعار موافقة على ملفات الارتباط، وتوضح البنية الحالية أن التحليلات غير الضرورية لا تُحمّل قبل اختيار المستخدم. يجب مراجعة النصوص القانونية قبل الاستخدام التجاري النهائي لتتطابق تماماً مع الوظائف الفعلية للمنصة، خصوصاً أن بعض النصوص القديمة ما زالت تشير إلى «الأدوات» رغم أن الواجهة الحالية أصبحت منصة حملات مجتمعية.

## 13. التقنية المستخدمة

المشروع مبني على React 19 وTypeScript وVite وTailwind CSS 4 وExpress وWouter وLucide React. توجد بنية خادم وطبقة tRPC وDrizzle ضمن قالب المشروع، لكن وظائف الحملات الحالية في الواجهة لا تستخدم قاعدة بيانات أو API للحفظ المركزي.

يحتوي ملف الحزم أيضاً على اعتماديات تاريخية مرتبطة بالأدوات السابقة، مثل `pdf-lib` و`browser-image-compression` و`@imgly/background-removal`. هذه الاعتماديات لا تظهر في مسارات التوجيه الحالية، ويمكن تنظيفها لاحقاً بعد التأكد من عدم وجود استخدامات مخفية وبناء نسخة منفصلة للتحقق.

## 14. الاختبارات المنفذة ونتائجها

تم تشغيل أوامر التحقق الحالية بعد تنظيف العبارات التجريبية وإعادة تسمية البيانات الداخلية:

| الاختبار أو الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح، دون أخطاء TypeScript |
| `pnpm test` | ناجح: 6 ملفات اختبار و8 اختبارات |
| `pnpm test:browser` | ناجح: 5 اختبارات Playwright |
| `pnpm build` | ناجح، تم إنشاء بناء الإنتاج |
| اختبار الصفحة الرئيسية | ناجح: التغذية والبحث والتصنيفات والدعوة للنشر |
| اختبار التبرع بالدم | ناجح: الاستجابة وتغيير الحالة |
| اختبار التطوع | ناجح: عرض الحملات ورابط نشر الفرصة |
| اختبار نشر الحملة | ناجح: التحقق والإرسال للمراجعة |
| اختبار تفاصيل الحملة | ناجح: التسجيل والإلغاء |
| اختبار الهاتف | ناجح: التنقل وعدم وجود تمرير أفقي |
| اختبار المسارات القديمة | ناجح: صفحة 404 وعدم ظهور الأدوات القديمة |
| التحقق البصري | ناجح للصفحة الرئيسية وصفحتي النشر على سطح المكتب |

ظهر تحذير غير حرج أثناء البناء لأن حزمة JavaScript الرئيسية تتجاوز 500KB بعد التصغير. كما يظهر تحذير من pnpm بأن حقل `pnpm` القديم في `package.json` لم يعد يُقرأ بالطريقة القديمة؛ لم يمنع ذلك الفحص أو البناء.

## 15. حالة الجاهزية الحالية

**حالة الواجهة:** جيدة للمراجعة والعرض وتجربة تدفقات الاستخدام الأساسية.  
**حالة الإنتاج متعددة المستخدمين:** غير مكتملة؛ لا توجد بعد قاعدة بيانات للحملات أو تسجيل دخول أو مراجعة إدارية حقيقية.  
**حالة النشر:** النسخة الأخيرة محفوظة ومنشورة تلقائياً في بيئة WebDev بالإصدار `3f12ee12`.  
**حالة الاستضافة الخارجية:** لم يتم تنفيذ نشر Hostinger.

## 16. حالة GitHub الفعلية

الحالة المحلية نظيفة، والفرع المحلي `main` عند الإصدار الكامل:

`3f12ee122c5285b173dccafb8c2e14797bf7361d`

لكن فحص GitHub الفعلي أظهر أن `main` في المستودع `alihkime20-cpu/ali40` ما زال عند:

`d4d78667026f194319bfbc330ac5691a198e1c77`

لذلك لا يمكن اعتبار GitHub main متزامناً مع آخر نسخة SABACUN المنشورة `3f12ee12` قبل تنفيذ مزامنة ودفع منفصل. لم أغيّر GitHub أثناء إعداد هذا التقرير.

## 17. التوصيات العملية

1. ربط الحملات والتسجيلات بقاعدة بيانات حقيقية مع نظام مستخدمين، حتى لا تبقى البيانات محصورة في جهاز واحد.
2. إضافة لوحة مراجعة إدارية قبل نشر الحملات للعامة، مع حالات واضحة للقبول والرفض والتعديل.
3. تحديث صفحات الخصوصية و«من نحن» لتتحدث عن منصة الحملات الحالية بدلاً من الإشارات القديمة إلى الأدوات، ثم تنظيف الاعتماديات غير المستخدمة وتحسين تقسيم الحزمة.
4. مزامنة الإصدار المحلي `3f12ee122c5285b173dccafb8c2e14797bf7361d` مع GitHub main قبل أي نشر خارجي إلى Hostinger.

---

**الخلاصة:** SABACUN حالياً واجهة عربية متجاوبة ومصممة بعناية لمنصة تبرع بالدم وتطوع، مع تدفقات استخدام أساسية تعمل محلياً ونتائج اختبار ناجحة. الجزء المتبقي للوصول إلى منصة إنتاج حقيقية هو بناء طبقة البيانات والمصادقة والمراجعة والمزامنة المركزية، وليس إعادة تصميم الواجهة.
